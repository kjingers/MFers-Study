/**
 * Simple test to verify API test setup works.
 */
import { describe, expect, it } from "vitest"

describe("API Test Setup", () => {
  it("should run a basic test", () => {
    expect(1 + 1).toBe(2)
  })
})
